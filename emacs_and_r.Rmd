---
title: "Using emacs and R for statistical analysis"
author: "Paul Robinson"
date: "`r format(Sys.Date())`"
output:
  html_document:
    keep_md: true

```{r, echo = F, cache = F}
knitr::opts_chunk$set(fig.retina = 2.5)
knitr::opts_chunk$set(fig.align = "center")
options(width = 100)
```

```{r, warning = F, message = F}
library(tidyverse)
library(abd)
library(brms)
library(broom.mixed)
library(posterior) # do I need this?
library(patchwork)

lion_noses <- as.data.frame(LionNoses) |> 
  mutate(percentage.black = 100*proportion.black)
lion_noses
```
## Why emacs

## Installling emacs

## What are the init file and elpa folder?

## Setting up ESS and supporting packages

## Working example and workflow
